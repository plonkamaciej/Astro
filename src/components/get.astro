---
interface Props {
	title?: string;
	href?: string;
}

const { href, title} = Astro.props;
---


 <a href={href}><button class="p-4 rounded-xl bg-green-600 text-white hover:bg-green-700">{title}</button></a>
